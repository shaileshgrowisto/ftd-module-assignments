Create a container component using `ftd.column` and with two child components `ftd.text`.

Add some dummy text in `ftd.text` variables to align as siblings

Example Code:

-- ftd.column:

;; <Child components>

-- end: ftd.column
